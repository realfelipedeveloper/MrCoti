# Web — exemplo de ambiente local
#
# Fonte principal: ../../.env.example
# Este arquivo existe apenas para documentar variáveis consumidas pelo frontend.

NODE_ENV=development
WEB_PORT=3400
NEXT_PUBLIC_API_BASE_URL=http://localhost:3200/api/v1
MRCOTI_PUBLIC_APP_NAME=Mr Coti
